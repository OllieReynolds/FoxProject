#pragma once
#include <string>

namespace fox {
    namespace utils {
        namespace RocketTracks {
            const std::string GLOBAL_LIGHT_INTENSITY = "global_tracks.rocket";
            const std::string SCENE_SPECIFIC_PARAMETER = "FoxScene_tracks.rocket";
        }
    }
}
